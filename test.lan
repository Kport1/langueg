var x : ({x:u32,y:(u16,u16)},u8) = ({.y=(1u8,2u16)},3u8);
/*var s : Shape<u32> = {.circle = (.rad = 6u32)}

var r = match(s)
    case .circle circle => 3u32 * circle.rad * circle.rad
    case .rect rect => rect.width * rect.height
    case .square square => square.sideLen * square.sideLen
    else => 10000u32

var x = &r;

type<T> Shape = {
    circle : (rad : T),
    rect : (width : T, height : T),
    square : (sideLen : T)
}*/

/*var x = (1u8, 2u8)
var y = (3u8, &x)
var z = &y;
(*(*z).1).0 = 4u8

var a = (1000u64, 2000u64)*/

/*var i = 0u32;
while(i < 10u32) {
    var x = &i;
    i += 1u32;
}*/

/*fn x((a : i32) -> i32 -> i32){
    return fn((b : i32) -> i32) a+b;
}

x(5)(3)*/

/*var x = (1u8,2u8,(3u8,4u8))
x.2.0=5*/



/*fn fib((n: u64) -> u64){
    if(n == 0u64) return 0u64
    if(n == 1u64) return 1u64
    return fib(n - 2u64) + fib(n - 1u64)
}

fib(40u64)*/


/*type<T> List = (
    val : T,
    next : {
        some : &List<T>,
        end : (),
    }
)

var l : List<u32> = (5u32, {.end = ()})
var l2 : List<u32> = (6u32, {.some = &l})*/

/*type B = f32
type A = (a : u32, b : B)
{
    type B = f64
    var x : A = (.a = 5u32, .b = 5f32)
}*/